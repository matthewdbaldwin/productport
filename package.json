{
  "name": "productport-api",
  "version": "0.11.0",
  "private": true,
  "description": "ProductPort API — a MicroPort satellite.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js",
    "migrate": "node scripts/db-migrate.js",
    "seed": "node prisma/seed.js",
    "test": "jest",
    "test:ci": "CI=true jest",
    "lint": "eslint src",
    "help:audit": "node scripts/help-audit.js",
    "help:media": "node tools/help-media/build.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1078.0",
    "@aws-sdk/s3-request-presigner": "^3.1078.0",
    "@matthewdbaldwin/microport-auth": "^0.16.3",
    "@matthewdbaldwin/microport-contracts": "^0.20.0",
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "csv-parse": "^5.6.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.2.0",
    "pino": "^9.3.2",
    "pino-http": "^10.3.0",
    "prisma": "^7.8.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "overrides": {
    "deepmerge-ts": "^8.0.2",
    "fast-uri": "^4.1.4",
    "mysql2": "^3.24.3"
  },
  "//deps-note": "prisma CLI lives in dependencies on purpose — runtime does npm ci --omit=dev then npx prisma migrate deploy. feedback_prisma_cli_stays_in_dependencies. NO file: self-deps here — feedback_develop_to_main_bump_breaks_web_lockfile.",
  "//overrides-note": "Forces patched versions of 3 High-severity CVEs nested under the prisma CLI's own dependency tree (deepmerge-ts stack-exhaustion DoS via @prisma/config, mysql2 auth-downgrade + decompression-bomb DoS, fast-uri SSRF/host-confusion via ajv/@prisma/streams-local). None of these three are ever exercised at runtime by this app (Postgres-only, no MySQL, no Prisma streams) — prisma itself has no stable release yet that bumps them (latest GA is 7.9.1; the fix lands in 8.0.0, still an RC as of 2026-09-03). feedback_prisma_transitive_cve_overrides."
}
